{
  "private": true,
  "name": "transpiling_python_to_python_talk",
  "scripts": {
    "start": "mdx-deck deck.mdx",
    "build:assets": "cp -r assets dist/",
    "build": "mdx-deck build deck.mdx --no-html && npm run build:assets",
    "image:min": "imagemin --out-dir assets assets"
  },
  "devDependencies": {
    "imagemin-cli": "^7.0.0",
    "mdx-deck": "^1.10.0",
    "mdx-deck-code-surfer": "^0.5.5",
    "raw-loader": "^4.0.2"
  },
  "dependencies": {
    "prism-react-renderer": "^2.1.0"
  }
}
